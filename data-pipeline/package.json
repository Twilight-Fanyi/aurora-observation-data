{
  "name": "aurora-data-pipeline",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=18.20"
  },
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "build": "node src/cli.mjs",
    "verify": "node src/cli.mjs --verify-only"
  },
  "dependencies": {
    "ajv": "8.20.0"
  }
}
